[build-system]
requires=["setuptools>=69","wheel"]
build-backend="setuptools.build_meta"
[project]
name="mutate4c"
version="0.1.0"
description="Mutation testing for C projects"
readme="README.md"
requires-python=">=3.11"
license={text="MIT"}
[project.scripts]
mutate4c="mutate4c.cli:main"
[tool.setuptools.packages.find]
where=["src"]
[tool.pytest.ini_options]
testpaths=["tests"]
